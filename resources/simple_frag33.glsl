#version 330 core
out vec3 color;

void main()
{
	color = vec3(1.0, 1.0, 0.0);
//	if ((gl_FragCoord.x == -1 && gl_FragCoord.y == -1) || (gl_FragCoord.x == 1 && gl_FragCoord.y == -1) || (gl_FragCoord.x == 0 && gl_FragCoord.y == 1)){
//		color = vec3(1.0, 1.0, 0.0);
//	}
//	else {
//		color = vec3(1.0, 1.0, 1.0);
//	}	
}
